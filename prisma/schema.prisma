generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

enum Role {
  USER
  STAFF
  ADMIN
}

model User {
  id             String          @id @default(auto()) @map("_id") @db.ObjectId
  name           String?
  email          String?         @unique
  emailVerified  DateTime?
  hashedPassword String?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  image          String?
  accounts       Account[]
  passwordResets PasswordReset[]
  role           Role            @default(STAFF) // Default role for User is STAFF
  employees      Employee[]      // Relation to Employee
}



model Admin {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  employeeId String   @db.ObjectId @unique // Ensure unique constraint for one-to-one relation
  role       Role     @default(ADMIN) // Default role for Admin is ADMIN
}
model Employee {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  name       String
  userId     String   @db.ObjectId
  role       Role     @default(STAFF) // Default role for Employee is STAFF
  user       User     @relation(fields: [userId], references: [id])
  adminId    String?  @db.ObjectId @unique // Ensure unique constraint for one-to-one relation
}
model Customer {
  id          String        @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  email       String        @unique
  phoneNumber String?
  address     String
  userId      String        @db.ObjectId
  role        Role          @default(USER) // Default role for Customer is USER
  orders      Order[]       // Define relation to Order
}

model Subscriber {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  email     String   @unique
  createdAt DateTime @default(now())
  role      Role     @default(USER) // Default role for Subscriber is USER
}
model Account {
  id                String  @id @default(auto()) @map("_id") @db.ObjectId
  userId            String  @db.ObjectId
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.String
  access_token      String? @db.String
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.String
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model PasswordReset {
  id     String   @id @default(auto()) @map("_id") @db.ObjectId
  token  String   @unique
  expiry DateTime
  userId String   @db.ObjectId
  user   User     @relation(fields: [userId], references: [id])
}

model Product {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  name        String
  description String
  price       Float
  oldPrice    Float?
  ratings     Float    @default(0)
  image       String?
  quantity    Int?     @default(0)
  createdAt   DateTime @default(now())
  category    String?  // Ensure this matches your code
}

model Order {
  id          String        @id @default(auto()) @map("_id") @db.ObjectId
  customerId  String        @db.ObjectId
  status      OrderStatus  @default(PENDING)
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt
  customer    Customer     @relation(fields: [customerId], references: [id]) // Define the relation
  items       OrderItem[]  // Relation to OrderItem
}

model OrderItem {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  orderId   String   @db.ObjectId
  productId String   @db.ObjectId
  quantity  Int
  price     Float
  order     Order    @relation(fields: [orderId], references: [id])
}

enum OrderStatus {
  DELIVERED
  PENDING
  PAID
}
